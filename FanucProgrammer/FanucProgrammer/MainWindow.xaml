<Window x:Class="FanucProgrammer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fanuc Programmer" Height="750" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF8C00"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF7F50"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF4500"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#0000CD"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4169E1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#191970"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Card Style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="3" BlurRadius="8" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="VerticalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Extended"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="True"/>
            <Setter Property="CanUserDeleteRows" Value="True"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header with Gradient -->
        <Border Grid.Row="0" Background="#FF8C00" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Fanuc Robot Programmer" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White"
                          VerticalAlignment="Center"/>
                <Menu Grid.Column="1" Background="Transparent" Foreground="White">
                    <MenuItem Header="Симуляция" Foreground="Black">
                        <MenuItem Header="Открыть окно эмуляции" Click="SimulateMenuItem_Click"/>
                        <MenuItem Header="Открыть окно 'Точки по модели'" Click="PointsOnModelMenuItem_Click"/>
                        <MenuItem Header="Открыть окно 'Конвертация текста" Click="TextToPointsGenerator_Click"/>
                        <MenuItem Header="Текст" Click="FanucTextGenerator_Click"/>
                    </MenuItem>
                </Menu>
            </Grid>
        </Border>
        
        <!-- Program Info Section -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="10,10,10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="Название программы:" 
                          Grid.Column="0" 
                          VerticalAlignment="Center" 
                          Margin="0,0,15,0"
                          FontWeight="SemiBold"
                          FontSize="14"/>
                <TextBox x:Name="ProgramNameTextBox" 
                        Grid.Column="1" 
                        Style="{StaticResource ModernTextBox}"
                        Margin="0,0,20,0"/>
                
                <TextBlock Text="Комментарий:" 
                          Grid.Column="2" 
                          VerticalAlignment="Center" 
                          Margin="0,0,15,0"
                          FontWeight="SemiBold"
                          FontSize="14"/>
                <TextBox x:Name="CommentTextBox" 
                        Grid.Column="3" 
                        Style="{StaticResource ModernTextBox}"
                        TextWrapping="Wrap" 
                        AcceptsReturn="True" 
                        VerticalScrollBarVisibility="Auto"
                        MinHeight="40"/>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Generated Code -->
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Сгенерированный код" 
                              FontSize="18" 
                              FontWeight="Bold" 
                              Margin="0,0,0,15"
                              Foreground="#333"/>
                    <Border Grid.Row="1" 
                           Background="#1E1E1E" 
                           CornerRadius="4" 
                           Padding="15"
                           BorderThickness="1"
                           BorderBrush="#333">
                        <ListBox x:Name="GeneratedCodeListBox" 
                                Background="Transparent"
                                Foreground="#D4D4D4"
                                BorderThickness="0"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                FontFamily="Consolas" 
                                FontSize="12"/>
                    </Border>
                </Grid>
            </Border>
            
            <!-- Right Panel - Controls -->
            <StackPanel Grid.Column="2">
                <!-- Points Configuration Card -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Настройки точек" 
                                  FontSize="18" 
                                  FontWeight="Bold" 
                                  Margin="0,0,0,15"
                                  Foreground="#333"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Количество точек:" 
                                      Grid.Column="0" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,15,0"
                                      FontWeight="SemiBold"/>
                            <TextBox x:Name="PointsCountTextBox" 
                                    Grid.Column="1" 
                                    Style="{StaticResource ModernTextBox}"
                                    Margin="0,0,15,0"/>
                            <Button Content="XYZ" 
                                   x:Name="XYZButton" 
                                   Grid.Column="2"
                                   Style="{StaticResource ModernButton}"
                                   Width="80"
                                   Margin="0,0,10,0"
                                   Click="XYZButton_Click"/>
                            <Button Content="J" 
                                   x:Name="JButton" 
                                   Grid.Column="3"
                                   Style="{StaticResource ModernButton}"
                                   Width="80"
                                   Click="JButton_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Points DataGrid Card -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,10">
                    <DataGrid x:Name="PointsDataGrid" 
                             Style="{StaticResource ModernDataGrid}"
                             Height="250"
                              CanUserAddRows="False"/>
                </Border>
                
                <!-- Generate Code Lines Button -->
                <Button Content="Сгенерировать строки кода" 
                       x:Name="GenerateCodeLinesButton" 
                       Style="{StaticResource ModernButton}"
                       Margin="10,0,10,10"
                       Click="GenerateCodeLinesButton_Click"/>
                
                <!-- Point Names DataGrid Card -->
                <Border Style="{StaticResource Card}">
                    <DataGrid x:Name="PointNamesDataGrid" 
                             Style="{StaticResource ModernDataGrid}"
                             Height="250"
                              CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="100"/>
                            <DataGridTextColumn Header="Наименование точки" Binding="{Binding PointName}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </Grid>
        
        <!-- Action Buttons -->
        <Border Grid.Row="3" Background="White" Padding="20,15" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сгенерировать код" 
                       x:Name="GenerateCodeButton" 
                       Style="{StaticResource PrimaryButton}"
                       Width="200" 
                       Height="45"
                       Margin="0,0,15,0"
                       Click="GenerateCodeButton_Click"/>
                <Button Content="Сохранить" 
                       x:Name="SaveButton" 
                       Style="{StaticResource PrimaryButton}"
                       Width="200" 
                       Height="45"
                       Click="SaveButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
